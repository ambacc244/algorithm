import java.util.*;

ArrayList<Integer> list = new ArrayList<Integer>();

list.add(1);  //1을 ArrayList에 넣음
list.get(0);  //index 0인 값을 return
list.remove(0);  //index 0인 값을 return
list.contains(2);  //list가 2를 포함하고 있으면 true, 아님 false
